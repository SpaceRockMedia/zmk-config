[project]
name = "zmk-config"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aider-chat>=0.74.2",
    "aider-install>=0.1.3",
    "flask>=3.1.0",
    "ruff>=0.9.7",
]
