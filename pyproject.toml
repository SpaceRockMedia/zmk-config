[project]
name = "zmk-config"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pip>=26.0.1",
    "west>=1.5.0",
    "wheel>=0.46.3",
]

[tool.uv.workspace]
members = [
    "zmk",
]
